import styled from 'styled-components';

const Search = () => {

  return (
    <Styled.Container>
      Search
    </Styled.Container>
  )
}

const Styled = {
  Container: styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
  `
}

export default Search;