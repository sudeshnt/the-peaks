import styled from 'styled-components';

export const RightContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  padding: 20px 0;
  align-items: flex-end;

  .btn {
    flex: 1;
    margin: 0 10px;
  }

  .order-picker {
    flex: 1;
  };
`;
